name: CI/CD Deployment

on:
  push:
    branches:
      - main  # Déploiement uniquement pour la branche principale

jobs:
  # Job 1: Déploiement du frontend sur Vercel
  deploy_frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Deploy to Vercel 🌐
        env:
          AUTH_TOKEN_VERCEL: ${{ secrets.AUTH_TOKEN_VERCEL }}
        run: |
          npm install -g vercel
          cd frontend
  
          DEPLOY_URL=$(vercel --prod --token $AUTH_TOKEN_VERCEL --yes --confirm --cwd .)
          echo "✅ Application déployée ! URL publique : $DEPLOY_URL"
  # Job 2: Déploiement du backend sur Render
  # deploy_backend:
  #   runs-on: ubuntu-latest
  #   needs: deploy_frontend

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: '3.10'

  #     - name: Install backend dependencies
  #       run: |
  #         cd api
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt

  #     - name: Deploy to Render
  #       run: |
  #         curl -X POST https://api.render.com/deploy/srv-cvokcv3uibrs73bsv350?key=vKoUqfz2XYE

  # vercel --prod --token $AUTH_TOKEN_VERCEL --yes --confirm --cwd .