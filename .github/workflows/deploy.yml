name: CI/CD Deployment

on:
  push:
    branches:
      - main  # Déploiement uniquement pour la branche principale

jobs:
  # Job 1: Déploiement du frontend sur Vercel Turibio
  deploy_frontend_turibio_vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Deploy to Vercel 🌐
        env:
          AUTH_TOKEN_VERCEL: ${{ secrets.AUTH_TOKEN_VERCEL }}
        run: |
          npm install -g vercel
          cd frontend
          DEPLOY_URL=$(vercel --prod --token $AUTH_TOKEN_VERCEL --yes --confirm --cwd .)
          echo "✅ Application Turibio déployée ! URL : $DEPLOY_URL"

  # Job 2: Déploiement du frontend sur Vercel Lionel (après Turibio)
  deploy_frontend_lionel_vercel:
    needs: deploy_frontend_turibio_vercel  # ➜ dépend de Turibio
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Deploy to Vercel 🌐
        env:
<<<<<<< HEAD
          AUTH_TOKEN_VERCEL: ${{ secrets.AUTH_TOKEN_VERCEL_LIONEL }}
=======
          AUTH_TOKEN_VERCEL_LIONEL: ${{ secrets.AUTH_TOKEN_VERCEL_LIONEL }}
>>>>>>> 47b6a34b18decfa3a12f775b0f75fcfcf82d6e61
        run: |
          npm install -g vercel
          cd frontend
          DEPLOY_URL=$(vercel --prod --token $AUTH_TOKEN_VERCEL_LIONEL --yes --confirm --cwd .)
          echo "✅ Application Lionel déployée ! URL : $DEPLOY_URL"


